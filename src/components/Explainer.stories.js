import React from "react"
import Explainer from "./Explainer"

export default {
  title: "Components/Explainer",
  component: Explainer,
}

const Template = args => <Explainer {...args} />

export const Default = Template.bind({})
